import { SchemaToLayout, schemaToLayout } from './layout_creator.js'

export default {
  install (Vue, options) {
    options = {
      ...options
    }
  }
}
export {
  SchemaToLayout,
  schemaToLayout
}
